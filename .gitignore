# OS / filesystem
.DS_Store
.AppleDouble
.LSOverride
._*
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# Editor / IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
*.bak
*.orig
*.rej

# Agents / AI tooling
.claude/
.aider*
.cursor/
.cursorignore

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Node — the project has no build step (see CLAUDE.md). If dev tooling
# ever pulls dependencies, ignore the install dir only. Lock files
# (package-lock.json, yarn.lock, pnpm-lock.yaml) should be COMMITTED
# for reproducibility per industry best practice.
node_modules/

# Build / output directories (none today; defensive)
dist/
build/
out/
.cache/
tmp/

# Test / coverage artefacts
coverage/
.nyc_output/
